const by_name = require('../models/by_name')
const delete_id = require('../models/delete_id')
const form_update = require('../models/form_update')
const update_by_id = require('../models/update_by_id')
const by_id = require('../models/by_id')
const form_create = require('../models/form_create')
const all = require('../models/all')
const post_data = require('../models/post_data')


module.exports = {
    by_name,
    by_id,
    form_create,
    post_data,
    all,
    delete_id,
    form_update,
    update_by_id,

}

